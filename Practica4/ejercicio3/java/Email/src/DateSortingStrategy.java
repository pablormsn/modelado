import java.util.List;

public class DateSortingStrategy implements Sorting{
    private void sort() {
        for (int i = 2; i <= email.size(), i++ )
        for (int j = email.size(); j >= i; j-- )
            if (before(email.at(j),email.at(j-1)) )
        // intercambiar los mensajes j y j-1
 ...
    }
}
